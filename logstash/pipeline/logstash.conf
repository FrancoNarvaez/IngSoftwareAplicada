input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # Add service name if provided; otherwise keep incoming fields
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "app-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
